'use client'

/** Light theme only — toggle hidden. */
export function ThemeToggle(_props: {
  variant?: 'ghost' | 'outline'
  size?: 'default' | 'sm' | 'lg' | 'icon' | 'icon-sm' | 'icon-lg'
  className?: string
  showLabel?: boolean
}) {
  return null
}
